get_filename_component(st_stream_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

if(NOT TARGET ST::stream)
    include("${st_stream_CMAKE_DIR}/st_streamTargets.cmake")
endif()

set(st_stream_LIBRARIES ST::stream)

